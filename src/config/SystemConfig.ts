//pc 状态下是否打开自动更新
export const isAutoUpdate: boolean = true;

//是否是加密发送消息
export const isCrypto: boolean = false;

// 接口地址(只修改此处即可)
export const apiUrl: string ="http://www.aitaotaoai8.site";
 
//节点的ip
export const apiSwitchUrl: string = "http://www.aitaotaoai8.site"; 

// 是否启用数据库
export const isOpenDB: boolean = true;

//是否开启web、pc、mac、h5统一为web
export const isOpenWeb: boolean = true;

//用于测试的代码（个别用户用）
export const isTest: boolean = false;

//打包是mac还win平台
export const isMac: boolean = false;

//是否打开日志上传
export const isOpenLogUpload: boolean=false;

//是否开启第二通道
export const isOpenTwoContent: boolean=false;

//是否打开双向撤回
export const isOpenDoubleDel: boolean = true;

//是否打开群内积分机制
export const isOpenIntegration: boolean = false;

//是否打开群内已读未读显示
export const isOpenRead: boolean = false;